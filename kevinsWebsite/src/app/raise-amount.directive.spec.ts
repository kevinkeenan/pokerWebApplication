import { RaiseAmountDirective } from './raise-amount.directive';

describe('RaiseAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new RaiseAmountDirective();
    expect(directive).toBeTruthy();
  });
});
