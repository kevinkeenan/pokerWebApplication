import { StartGameDirective } from './start-game.directive';

describe('StartGameDirective', () => {
  it('should create an instance', () => {
    const directive = new StartGameDirective();
    expect(directive).toBeTruthy();
  });
});
